#include <gtest/gtest.h>
#include "crddagt/common/common.hpp"

TEST(PlaceholderTests, SampleTest)
{
    std::cout << "Running placeholder test... (always passes)" << std::endl;
    EXPECT_TRUE(true);
}
